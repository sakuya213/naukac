#include <stdio.h>

main()
{
    int za; // what is this? - "za" it nothing tell about. Maybe counter? 
za=0;
do   
{
printf("%d\n",za);
za++;

}
while(za<=10);
getch();
return 0;
}

// always code style problem - as earlier for start PSR-1 you need to read
